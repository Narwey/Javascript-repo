<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Detail Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }

        h3 {
            text-align: center;
            margin-top: 20px;
            color: #333;
        }

        ul {
            list-style-type: none;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        li {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
            margin: 20px;
            padding: 20px;
            width: 300px;
            text-align: left;
        }

        li p {
            margin: 5px 0;
        }

        strong {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <h3>Product Details</h3>
    <ul>
        <li>
            <img src="/public/<%= products.image %>" alt="<%= products.name %> Image" style="max-width: 100%; height: auto; border: 1px solid #ddd; border-radius: 5px; box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);">
            <p><strong>Name:</strong> <%= products.name %></p>
            <p><strong>Price:</strong> <%= products.price %> Euro</p>
            <p><strong>Description:</strong> <%= products.description %></p>
        </li>
    </ul>
</body>
</html>
